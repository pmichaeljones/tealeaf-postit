<% if @vote.valid? %>
  <% @vote.save %>
  $("#post-<%= @post.id %>-number").html("<%= @post.total_votes %> Votes")
<% else %>
  $("#post-box-<%= @post.id %>").html("You already voted on this post.").addClass("alert alert-error")
  //alert("You already voted on that!")
<% end %>


